import { SerchPipePipe } from './serch-pipe.pipe';

describe('SerchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SerchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
